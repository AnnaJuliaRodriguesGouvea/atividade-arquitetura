import env from "react-dotenv";

export const AlunoDao = {
    inserir: function(aluno) {
        const data = {
            nome: aluno.nome,
            nota1: aluno.nota1,
            nota2: aluno.nota2,
            nota3: aluno.nota3,
            media: aluno.media
        }
    },

    listar: async function() {
        
    },
}